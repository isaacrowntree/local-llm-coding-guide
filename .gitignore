# Local secrets — never commit
.env.local
.env

# macOS
.DS_Store

# Python bytecode
__pycache__/
*.pyc

# LAN client config (contains the llama-server API key)
.llama-lan.conf

# real llama-server keys (only the .example template is tracked)
windows/api-keys.txt
api-keys.txt
api-key.txt
