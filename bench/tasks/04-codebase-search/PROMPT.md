Somewhere in this codebase there is a constant that controls the shard rebalance
threshold. Find it.

Write ONLY its numeric value into a file called ANSWER.txt in the repository root
(the number and nothing else, e.g. `0.5`). Do not change any other file.
